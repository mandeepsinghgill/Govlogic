from fastapi import APIRouter
from app.models.customer_portal import PortalAccess, DeliverableSubmission, CustomerFeedback, PerformanceReport, CustomerNotification, PortalActivity

router = APIRouter()

