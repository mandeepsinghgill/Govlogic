services:
  postgres:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: GovSure
      POSTGRES_PASSWORD: GovSure
      POSTGRES_DB: GovSure
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U GovSure"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: ../docker/Dockerfile.backend
    expose:
      - "8000"              # expose to the compose network only
    environment:
      DATABASE_URL: postgresql://GovSure:GovSure@postgres:5432/GovSure
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      DEBUG: "false"
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    volumes:
      - ./backend:/app
      - uploads:/tmp/GovSure/uploads
      - exports:/tmp/GovSure/exports
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  celery:
    build:
      context: ./backend
      dockerfile: ../docker/Dockerfile.backend
    environment:
      DATABASE_URL: postgresql://GovSure:GovSure@postgres:5432/GovSure
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    volumes:
      - ./backend:/app
      - uploads:/tmp/GovSure/uploads
    depends_on:
      - postgres
      - redis
    command: celery -A app.celery_app worker --loglevel=info

  # Frontend built → Caddy runtime (serves dist and proxies /api)
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
      args:
        # Use empty string to make API calls relative to current domain
        # Caddy will proxy /api/* to backend
        VITE_API_URL: ""
    depends_on:
      - backend

  caddy:
    image: caddy:2
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
      - backend
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
      # serve the built assets from the web image
      - type: bind
        source: ./frontend/dist
        target: /usr/share/caddy
        bind:
          create_host_path: true
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  uploads:
  exports:
  caddy_data:
  caddy_config: